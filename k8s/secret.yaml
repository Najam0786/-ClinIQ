# TEMPLATE — do NOT commit real secrets.
# Create from this template:
#   kubectl create secret generic cliniq-secrets -n cliniq \
#     --from-literal=JWT_SECRET_KEY=<your-secret> \
#     --from-literal=POSTGRES_PASSWORD=<your-password>
#
apiVersion: v1
kind: Secret
metadata:
  name: cliniq-secrets
  namespace: cliniq
type: Opaque
stringData:
  JWT_SECRET_KEY: "REPLACE_ME_with_a_long_random_string"
  POSTGRES_PASSWORD: "REPLACE_ME_with_a_strong_password"
